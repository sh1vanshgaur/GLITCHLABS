SNIPPETS = [{'id': 'py-medium-sliding-window-1',
  'language': 'Python',
  'difficulty': 'Medium',
  'category': 'sliding window',
  'title': 'Duplicate Window Drift',
  'problem_statement': 'Print the length of the longest substring without repeating characters. The bug is a realistic sliding-window pointer update mistake.',
  'buggy_code': 'text = "abba"\n\nleft = 0\nbest = 0\nlast_seen = {}\n\nfor right, ch in enumerate(text):\n    if ch in last_seen and last_seen[ch] >= left:\n        left += 1\n\n    last_seen[ch] = right\n    best = max(best, right - left + 1)\n\nprint(best)',
  'correct_code': 'text = "abba"\n\nleft = 0\nbest = 0\nlast_seen = {}\n\nfor right, ch in enumerate(text):\n    if ch in last_seen and last_seen[ch] >= left:\n        left = last_seen[ch] + 1\n\n    last_seen[ch] = right\n    best = max(best, right - left + 1)\n\nprint(best)',
  'bug_type': 'state_bug',
  'test_cases': [{'input': 'text = "abba"', 'expected_output': '2', 'actual_output': '3'},
                 {'input': 'text = "abcabcbb"', 'expected_output': '3', 'actual_output': '4'}],
  'explanation': 'When a duplicate reappears, moving `left` by only one step is not enough. The window must jump past the previous occurrence of that exact character.',
  'trace_steps': [{'line': 7,
                   'note': 'The second `b` appears while the window already contains `b`.',
                   'variables': {'right': 2, 'left': 0, "last_seen['b']": 1}},
                  {'line': 8,
                   'note': 'The left pointer moves by one instead of jumping to `2`.',
                   'variables': {'new_left': 1},
                   'suspicious': True},
                  {'line': 11,
                   'note': 'That leaves a duplicate inside the current window and inflates the answer.',
                   'variables': {'best': 3}}],
  'code': 'text = "abba"\n\nleft = 0\nbest = 0\nlast_seen = {}\n\nfor right, ch in enumerate(text):\n    if ch in last_seen and last_seen[ch] >= left:\n        left += 1\n\n    last_seen[ch] = right\n    best = max(best, right - left + 1)\n\nprint(best)',
  'fixed_code': 'text = "abba"\n\nleft = 0\nbest = 0\nlast_seen = {}\n\nfor right, ch in enumerate(text):\n    if ch in last_seen and last_seen[ch] >= left:\n        left = last_seen[ch] + 1\n\n    last_seen[ch] = right\n    best = max(best, right - left + 1)\n\nprint(best)'},
 {'id': 'java-hard-sliding-window-1',
  'language': 'Java',
  'difficulty': 'Hard',
  'category': 'sliding window',
  'title': 'Minimum Window Shrink Bug',
  'problem_statement': 'Print the length of the smallest substring containing `A`, `B`, and `C`. The bug appears when shrinking the window: the code drops the matched-count too aggressively.',
  'buggy_code': 'import java.util.HashMap;\nimport java.util.Map;\n\npublic class Main {\n    public static void main(String[] args) {\n        String text = "AAABBC";\n        Map<Character, Integer> need = new HashMap<>();\n        need.put(\'A\', 1);\n        need.put(\'B\', 1);\n        need.put(\'C\', 1);\n\n        Map<Character, Integer> have = new HashMap<>();\n        int formed = 0;\n        int left = 0;\n        int best = Integer.MAX_VALUE;\n\n        for (int right = 0; right < text.length(); right++) {\n            char current = text.charAt(right);\n            if (need.containsKey(current)) {\n                have.put(current, have.getOrDefault(current, 0) + 1);\n                if (have.get(current).intValue() == need.get(current).intValue()) {\n                    formed++;\n                }\n            }\n\n            while (formed == need.size()) {\n                best = Math.min(best, right - left + 1);\n                char leftChar = text.charAt(left);\n\n                if (need.containsKey(leftChar)) {\n                    have.put(leftChar, have.get(leftChar) - 1);\n                    formed--;\n                }\n                left++;\n            }\n        }\n\n        System.out.println(best);\n    }\n}',
  'correct_code': 'import java.util.HashMap;\nimport java.util.Map;\n\npublic class Main {\n    public static void main(String[] args) {\n        String text = "AAABBC";\n        Map<Character, Integer> need = new HashMap<>();\n        need.put(\'A\', 1);\n        need.put(\'B\', 1);\n        need.put(\'C\', 1);\n\n        Map<Character, Integer> have = new HashMap<>();\n        int formed = 0;\n        int left = 0;\n        int best = Integer.MAX_VALUE;\n\n        for (int right = 0; right < text.length(); right++) {\n            char current = text.charAt(right);\n            if (need.containsKey(current)) {\n                have.put(current, have.getOrDefault(current, 0) + 1);\n                if (have.get(current).intValue() == need.get(current).intValue()) {\n                    formed++;\n                }\n            }\n\n            while (formed == need.size()) {\n                best = Math.min(best, right - left + 1);\n                char leftChar = text.charAt(left);\n\n                if (need.containsKey(leftChar)) {\n                    have.put(leftChar, have.get(leftChar) - 1);\n                    if (have.get(leftChar) < need.get(leftChar)) {\n                        formed--;\n                    }\n                }\n                left++;\n            }\n        }\n\n        System.out.println(best);\n    }\n}',
  'bug_type': 'state_bug',
  'test_cases': [{'input': 'text = "AAABBC", need = {A, B, C}',
                  'expected_output': '4',
                  'actual_output': '5'},
                 {'input': 'text = "ABC", need = {A, B, C}',
                  'expected_output': '3',
                  'actual_output': '3'}],
  'explanation': 'When the left edge moves, `formed` should drop only if the window now has too few copies of that required character. Dropping it unconditionally stops shrinking too early.',
  'trace_steps': [{'line': 23,
                   'note': 'The first valid window is `AAABBC` with length `6`.',
                   'variables': {'left': 0, 'right': 5, 'best': 6}},
                  {'line': 28,
                   'note': 'Removing the first `A` still leaves enough `A`s, but the code decrements `formed` anyway.',
                   'variables': {"have['A']_after_decrement": 2},
                   'suspicious': True},
                  {'line': 35,
                   'note': 'The shrink loop stops at length `5`, missing the better window `ABBC`.',
                   'variables': {'printed': 5}}],
  'code': 'import java.util.HashMap;\nimport java.util.Map;\n\npublic class Main {\n    public static void main(String[] args) {\n        String text = "AAABBC";\n        Map<Character, Integer> need = new HashMap<>();\n        need.put(\'A\', 1);\n        need.put(\'B\', 1);\n        need.put(\'C\', 1);\n\n        Map<Character, Integer> have = new HashMap<>();\n        int formed = 0;\n        int left = 0;\n        int best = Integer.MAX_VALUE;\n\n        for (int right = 0; right < text.length(); right++) {\n            char current = text.charAt(right);\n            if (need.containsKey(current)) {\n                have.put(current, have.getOrDefault(current, 0) + 1);\n                if (have.get(current).intValue() == need.get(current).intValue()) {\n                    formed++;\n                }\n            }\n\n            while (formed == need.size()) {\n                best = Math.min(best, right - left + 1);\n                char leftChar = text.charAt(left);\n\n                if (need.containsKey(leftChar)) {\n                    have.put(leftChar, have.get(leftChar) - 1);\n                    formed--;\n                }\n                left++;\n            }\n        }\n\n        System.out.println(best);\n    }\n}',
  'fixed_code': 'import java.util.HashMap;\nimport java.util.Map;\n\npublic class Main {\n    public static void main(String[] args) {\n        String text = "AAABBC";\n        Map<Character, Integer> need = new HashMap<>();\n        need.put(\'A\', 1);\n        need.put(\'B\', 1);\n        need.put(\'C\', 1);\n\n        Map<Character, Integer> have = new HashMap<>();\n        int formed = 0;\n        int left = 0;\n        int best = Integer.MAX_VALUE;\n\n        for (int right = 0; right < text.length(); right++) {\n            char current = text.charAt(right);\n            if (need.containsKey(current)) {\n                have.put(current, have.getOrDefault(current, 0) + 1);\n                if (have.get(current).intValue() == need.get(current).intValue()) {\n                    formed++;\n                }\n            }\n\n            while (formed == need.size()) {\n                best = Math.min(best, right - left + 1);\n                char leftChar = text.charAt(left);\n\n                if (need.containsKey(leftChar)) {\n                    have.put(leftChar, have.get(leftChar) - 1);\n                    if (have.get(leftChar) < need.get(leftChar)) {\n                        formed--;\n                    }\n                }\n                left++;\n            }\n        }\n\n        System.out.println(best);\n    }\n}'}]
