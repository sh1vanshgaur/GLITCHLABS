SNIPPETS = [{'id': 'py-hard-runtime-1',
  'language': 'Python',
  'difficulty': 'Hard',
  'category': 'runtime',
  'title': 'Safe Profile Access',
  'problem_statement': None,
  'buggy_code': 'profile = {"name": "Ria", "skills": ["python", "fastapi"]}\n\nprint(profile["xp"])',
  'correct_code': 'profile = {"name": "Ria", "skills": ["python", "fastapi"]}\n\nprint(profile.get("xp", 0))',
  'bug_type': 'edge_case_issue',
  'test_cases': [{'input': '', 'expected_output': '0', 'actual_output': "KeyError: 'xp'"}],
  'explanation': 'The code assumes the `xp` key always exists. A guarded lookup handles the missing-key edge case safely.',
  'trace_steps': [{'line': 1,
                   'note': 'Build the profile dictionary.',
                   'variables': {'profile': '{"name": "Ria", "skills": ["python", "fastapi"]}'}},
                  {'line': 3,
                   'note': 'Direct dictionary access fails because `xp` is missing.',
                   'variables': {'lookup_key': '"xp"', 'error': 'KeyError'},
                   'suspicious': True}],
  'code': 'profile = {"name": "Ria", "skills": ["python", "fastapi"]}\n\nprint(profile["xp"])',
  'fixed_code': 'profile = {"name": "Ria", "skills": ["python", "fastapi"]}\n\nprint(profile.get("xp", 0))'},
 {'id': 'cpp-easy-hashing-1',
  'language': 'C++',
  'difficulty': 'Easy',
  'category': 'hashing',
  'title': 'Duplicate Detector Flip',
  'problem_statement': 'Print whether the array contains any duplicate value. The bug is in the hash-check logic, not in the data structure choice.',
  'buggy_code': '#include <iostream>\n#include <unordered_set>\n\nint main() {\n    int nums[] = {4, 1, 7, 9};\n    std::unordered_set<int> seen;\n    bool hasDuplicate = false;\n\n    for (int value : nums) {\n        if (seen.count(value) == 0) {\n            hasDuplicate = true;\n            break;\n        }\n        seen.insert(value);\n    }\n\n    std::cout << (hasDuplicate ? "true" : "false") << std::endl;\n    return 0;\n}',
  'correct_code': '#include <iostream>\n#include <unordered_set>\n\nint main() {\n    int nums[] = {4, 1, 7, 9};\n    std::unordered_set<int> seen;\n    bool hasDuplicate = false;\n\n    for (int value : nums) {\n        if (seen.count(value) == 1) {\n            hasDuplicate = true;\n            break;\n        }\n        seen.insert(value);\n    }\n\n    std::cout << (hasDuplicate ? "true" : "false") << std::endl;\n    return 0;\n}',
  'bug_type': 'condition_issue',
  'test_cases': [{'input': 'nums = {4, 1, 7, 9}',
                  'expected_output': 'false',
                  'actual_output': 'true'},
                 {'input': 'nums = {4, 1, 7, 4}',
                  'expected_output': 'true',
                  'actual_output': 'true'}],
  'explanation': 'The condition treats unseen values as duplicates. On the very first element, `seen.count(value)` is `0`, so the program incorrectly reports a duplicate immediately.',
  'trace_steps': [{'line': 5, 'note': 'The set starts empty.', 'variables': {'seen': '{}'}},
                  {'line': 9,
                   'note': 'For `4`, `seen.count(4)` is `0`, but the code treats that as a duplicate signal.',
                   'variables': {'value': 4, 'seen.count(value)': 0},
                   'suspicious': True},
                  {'line': 15,
                   'note': 'The program exits before inserting any values.',
                   'variables': {'hasDuplicate': 'true'}}],
  'code': '#include <iostream>\n#include <unordered_set>\n\nint main() {\n    int nums[] = {4, 1, 7, 9};\n    std::unordered_set<int> seen;\n    bool hasDuplicate = false;\n\n    for (int value : nums) {\n        if (seen.count(value) == 0) {\n            hasDuplicate = true;\n            break;\n        }\n        seen.insert(value);\n    }\n\n    std::cout << (hasDuplicate ? "true" : "false") << std::endl;\n    return 0;\n}',
  'fixed_code': '#include <iostream>\n#include <unordered_set>\n\nint main() {\n    int nums[] = {4, 1, 7, 9};\n    std::unordered_set<int> seen;\n    bool hasDuplicate = false;\n\n    for (int value : nums) {\n        if (seen.count(value) == 1) {\n            hasDuplicate = true;\n            break;\n        }\n        seen.insert(value);\n    }\n\n    std::cout << (hasDuplicate ? "true" : "false") << std::endl;\n    return 0;\n}'},
 {'id': 'py-medium-hashing-1',
  'language': 'Python',
  'difficulty': 'Medium',
  'category': 'hashing',
  'title': 'Two Sum Self-Match',
  'problem_statement': 'Print the indices of two numbers that add to the target. The buggy solution uses the hash map in the wrong order and accidentally reuses the current element.',
  'buggy_code': 'nums = [3, 3, 4]\ntarget = 6\n\nseen = {}\n\nfor index, value in enumerate(nums):\n    seen[value] = index\n    need = target - value\n\n    if need in seen:\n        print([seen[need], index])\n        break',
  'correct_code': 'nums = [3, 3, 4]\ntarget = 6\n\nseen = {}\n\nfor index, value in enumerate(nums):\n    need = target - value\n\n    if need in seen:\n        print([seen[need], index])\n        break\n\n    seen[value] = index',
  'bug_type': 'state_bug',
  'test_cases': [{'input': 'nums = [3, 3, 4], target = 6',
                  'expected_output': '[0, 1]',
                  'actual_output': '[0, 0]'},
                 {'input': 'nums = [2, 7, 11, 15], target = 9',
                  'expected_output': '[0, 1]',
                  'actual_output': '[0, 1]'}],
  'explanation': 'The current value is inserted before the lookup happens. When `value` equals `need`, the code finds the index it just stored and incorrectly pairs the element with itself.',
  'trace_steps': [{'line': 4,
                   'note': 'The hash map is empty before the loop starts.',
                   'variables': {'seen': '{}'}},
                  {'line': 7,
                   'note': 'At index `0`, the code stores `3 -> 0` before searching for the complement.',
                   'variables': {'index': 0, 'value': 3},
                   'suspicious': True},
                  {'line': 10,
                   'note': 'The lookup now finds the same index that was just written.',
                   'variables': {'need': 3, 'seen[need]': 0}}],
  'code': 'nums = [3, 3, 4]\ntarget = 6\n\nseen = {}\n\nfor index, value in enumerate(nums):\n    seen[value] = index\n    need = target - value\n\n    if need in seen:\n        print([seen[need], index])\n        break',
  'fixed_code': 'nums = [3, 3, 4]\ntarget = 6\n\nseen = {}\n\nfor index, value in enumerate(nums):\n    need = target - value\n\n    if need in seen:\n        print([seen[need], index])\n        break\n\n    seen[value] = index'}]
