SNIPPETS = [{'id': 'py-easy-binary-search-1',
  'language': 'Python',
  'difficulty': 'Easy',
  'category': 'binary search',
  'title': 'Final Slot Search',
  'problem_statement': 'Print whether the target exists in the sorted list. The bug is a common binary-search boundary mistake that skips the last remaining slot.',
  'buggy_code': 'nums = [1, 3, 5, 7, 9]\ntarget = 9\n\nleft = 0\nright = len(nums) - 1\nfound = False\n\nwhile left < right:\n    mid = (left + right) // 2\n\n    if nums[mid] == target:\n        found = True\n        break\n    if nums[mid] < target:\n        left = mid + 1\n    else:\n        right = mid - 1\n\nprint(found)',
  'correct_code': 'nums = [1, 3, 5, 7, 9]\ntarget = 9\n\nleft = 0\nright = len(nums) - 1\nfound = False\n\nwhile left <= right:\n    mid = (left + right) // 2\n\n    if nums[mid] == target:\n        found = True\n        break\n    if nums[mid] < target:\n        left = mid + 1\n    else:\n        right = mid - 1\n\nprint(found)',
  'bug_type': 'loop_issue',
  'test_cases': [{'input': 'nums = [1, 3, 5, 7, 9], target = 9',
                  'expected_output': 'True',
                  'actual_output': 'False'},
                 {'input': 'nums = [1, 3, 5, 7, 9], target = 5',
                  'expected_output': 'True',
                  'actual_output': 'True'}],
  'explanation': 'The loop stops when `left == right`, so the final candidate index is never checked. That misses targets sitting at the last remaining position.',
  'trace_steps': [{'line': 4,
                   'note': 'The search begins with the full range.',
                   'variables': {'left': 0, 'right': 4}},
                  {'line': 8,
                   'note': 'After narrowing, the pointers converge on index `4`.',
                   'variables': {'left': 4, 'right': 4}},
                  {'line': 8,
                   'note': 'Because the loop requires `left < right`, it exits before checking `nums[4]`.',
                   'variables': {'target_slot': 4},
                   'suspicious': True},
                  {'line': 18,
                   'note': 'The default `False` value is printed.',
                   'variables': {'found': False}}],
  'code': 'nums = [1, 3, 5, 7, 9]\ntarget = 9\n\nleft = 0\nright = len(nums) - 1\nfound = False\n\nwhile left < right:\n    mid = (left + right) // 2\n\n    if nums[mid] == target:\n        found = True\n        break\n    if nums[mid] < target:\n        left = mid + 1\n    else:\n        right = mid - 1\n\nprint(found)',
  'fixed_code': 'nums = [1, 3, 5, 7, 9]\ntarget = 9\n\nleft = 0\nright = len(nums) - 1\nfound = False\n\nwhile left <= right:\n    mid = (left + right) // 2\n\n    if nums[mid] == target:\n        found = True\n        break\n    if nums[mid] < target:\n        left = mid + 1\n    else:\n        right = mid - 1\n\nprint(found)'},
 {'id': 'java-medium-binary-search-1',
  'language': 'Java',
  'difficulty': 'Medium',
  'category': 'binary search',
  'title': 'Lower Bound Moves Wrong',
  'problem_statement': 'Print the first index whose value is at least the target. The intended solution is lower bound, but the search moves in the wrong direction after finding a candidate.',
  'buggy_code': 'public class Main {\n    public static void main(String[] args) {\n        int[] nums = {1, 2, 4, 4, 4, 7};\n        int target = 4;\n        int left = 0;\n        int right = nums.length - 1;\n        int answer = -1;\n\n        while (left <= right) {\n            int mid = (left + right) / 2;\n\n            if (nums[mid] >= target) {\n                answer = mid;\n                left = mid + 1;\n            } else {\n                left = mid + 1;\n            }\n        }\n\n        System.out.println(answer);\n    }\n}',
  'correct_code': 'public class Main {\n    public static void main(String[] args) {\n        int[] nums = {1, 2, 4, 4, 4, 7};\n        int target = 4;\n        int left = 0;\n        int right = nums.length - 1;\n        int answer = -1;\n\n        while (left <= right) {\n            int mid = (left + right) / 2;\n\n            if (nums[mid] >= target) {\n                answer = mid;\n                right = mid - 1;\n            } else {\n                left = mid + 1;\n            }\n        }\n\n        System.out.println(answer);\n    }\n}',
  'bug_type': 'condition_issue',
  'test_cases': [{'input': 'nums = {1, 2, 4, 4, 4, 7}, target = 4',
                  'expected_output': '2',
                  'actual_output': '4'},
                 {'input': 'nums = {1, 2, 4, 4, 4, 7}, target = 6',
                  'expected_output': '5',
                  'actual_output': '5'}],
  'explanation': 'Once a candidate index is found, lower bound should continue searching to the left. Moving `left` rightward finds the last matching index instead of the first one.',
  'trace_steps': [{'line': 9,
                   'note': 'The first match may land in the middle of the equal block.',
                   'variables': {'mid': 2, 'nums[mid]': 4}},
                  {'line': 12,
                   'note': 'The code records the candidate but searches right instead of left.',
                   'variables': {'answer': 2, 'left_after_update': 3},
                   'suspicious': True},
                  {'line': 19,
                   'note': 'Later matches overwrite the earlier correct answer.',
                   'variables': {'final_answer': 4}}],
  'code': 'public class Main {\n    public static void main(String[] args) {\n        int[] nums = {1, 2, 4, 4, 4, 7};\n        int target = 4;\n        int left = 0;\n        int right = nums.length - 1;\n        int answer = -1;\n\n        while (left <= right) {\n            int mid = (left + right) / 2;\n\n            if (nums[mid] >= target) {\n                answer = mid;\n                left = mid + 1;\n            } else {\n                left = mid + 1;\n            }\n        }\n\n        System.out.println(answer);\n    }\n}',
  'fixed_code': 'public class Main {\n    public static void main(String[] args) {\n        int[] nums = {1, 2, 4, 4, 4, 7};\n        int target = 4;\n        int left = 0;\n        int right = nums.length - 1;\n        int answer = -1;\n\n        while (left <= right) {\n            int mid = (left + right) / 2;\n\n            if (nums[mid] >= target) {\n                answer = mid;\n                right = mid - 1;\n            } else {\n                left = mid + 1;\n            }\n        }\n\n        System.out.println(answer);\n    }\n}'},
 {'id': 'cpp-hard-binary-search-1',
  'language': 'C++',
  'difficulty': 'Hard',
  'category': 'binary search',
  'title': 'Stuck Insert Search',
  'problem_statement': 'Find the insert position for the target in the sorted array. The bug is a hard binary-search loop that stops making progress on a two-element interval.',
  'buggy_code': '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums = {1, 3, 5, 7, 9};\n    int target = 2;\n    int left = 0;\n    int right = nums.size() - 1;\n\n    while (left < right) {\n        int mid = (left + right + 1) / 2;\n\n        if (nums[mid] >= target) {\n            right = mid;\n        } else {\n            left = mid;\n        }\n    }\n\n    std::cout << left << std::endl;\n    return 0;\n}',
  'correct_code': '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums = {1, 3, 5, 7, 9};\n    int target = 2;\n    int left = 0;\n    int right = nums.size() - 1;\n    int answer = nums.size();\n\n    while (left <= right) {\n        int mid = (left + right) / 2;\n\n        if (nums[mid] >= target) {\n            answer = mid;\n            right = mid - 1;\n        } else {\n            left = mid + 1;\n        }\n    }\n\n    std::cout << answer << std::endl;\n    return 0;\n}',
  'bug_type': 'loop_issue',
  'test_cases': [{'input': 'nums = {1, 3, 5, 7, 9}, target = 2',
                  'expected_output': '1',
                  'actual_output': 'Infinite loop'},
                 {'input': 'nums = {1, 3, 5, 7, 9}, target = 9',
                  'expected_output': '4',
                  'actual_output': '4'}],
  'explanation': 'When `left = 0` and `right = 1`, `mid` becomes `1`. If `nums[mid] >= target`, setting `right = mid` leaves the interval unchanged, so the loop can stall forever.',
  'trace_steps': [{'line': 9,
                   'note': 'The search narrows down to the interval `[0, 1]`.',
                   'variables': {'left': 0, 'right': 1}},
                  {'line': 10,
                   'note': 'The biased midpoint stays at `1`.',
                   'variables': {'mid': 1}},
                  {'line': 13,
                   'note': 'Setting `right = mid` leaves the interval unchanged, so the loop repeats forever.',
                   'variables': {'left': 0, 'right': 1},
                   'suspicious': True}],
  'code': '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums = {1, 3, 5, 7, 9};\n    int target = 2;\n    int left = 0;\n    int right = nums.size() - 1;\n\n    while (left < right) {\n        int mid = (left + right + 1) / 2;\n\n        if (nums[mid] >= target) {\n            right = mid;\n        } else {\n            left = mid;\n        }\n    }\n\n    std::cout << left << std::endl;\n    return 0;\n}',
  'fixed_code': '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums = {1, 3, 5, 7, 9};\n    int target = 2;\n    int left = 0;\n    int right = nums.size() - 1;\n    int answer = nums.size();\n\n    while (left <= right) {\n        int mid = (left + right) / 2;\n\n        if (nums[mid] >= target) {\n            answer = mid;\n            right = mid - 1;\n        } else {\n            left = mid + 1;\n        }\n    }\n\n    std::cout << answer << std::endl;\n    return 0;\n}'}]
